# Explique quando usar o del e quando usar o pop()

# Quando utilizarmos o del vamos deletar definitivamente na lista, e quando usarmos o pop() 
# vamos deletar os dados mas vamos poder utilizalos novamente. 